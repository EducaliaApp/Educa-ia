name: Sync Rúbricas MINEDUC

on:
  schedule:
    # Ejecutar los domingos a las 2 AM UTC (11 PM Chile)
    - cron: '0 2 * * 0'

  workflow_dispatch:
    # Permitir ejecución manual
    inputs:
      force_full_sync:
        description: 'Forzar sincronización completa (ignora cache)'
        required: false
        default: false
        type: boolean
      slack_notification:
        description: 'Enviar notificación por Slack'
        required: false
        default: false
        type: boolean
      force_rubric_extraction:
        description: 'Forzar extracción de rúbricas con IA'
        required: false
        default: false
        type: boolean

env:
  ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
  OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
  SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}

jobs:
  monitor-documentos:
    name: Monitorear Documentos Oficiales
    runs-on: ubuntu-latest
    timeout-minutes: 30
    outputs:
      has_changes: ${{ steps.check_nuevos.outputs.has_changes }}
      nuevos: ${{ steps.check_nuevos.outputs.nuevos }}
      actualizados: ${{ steps.check_nuevos.outputs.actualizados }}

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.40.x

      - name: Invocar Edge Function de monitoreo
        id: monitor
        run: |
          response=$(curl -X POST \
            "${{ secrets.SUPABASE_URL }}/functions/v1/monitor-documentos-oficiales" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json" \
            -d '{"force": ${{ github.event.inputs.force_full_sync || false }}}')

          echo "response=$response" >> $GITHUB_OUTPUT
          echo "### 📊 Reporte de Monitoreo" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`json" >> $GITHUB_STEP_SUMMARY
          echo "$response" | jq '.' >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY

      - name: Verificar documentos nuevos
        id: check_nuevos
        run: |
          response='${{ steps.monitor.outputs.response }}'

          # Verificar si hay error en la respuesta
          if echo "$response" | jq -e '.error' > /dev/null; then
            echo "❌ Error en Edge Function: $(echo "$response" | jq -r '.error')"
            echo "nuevos=0" >> $GITHUB_OUTPUT
            echo "actualizados=0" >> $GITHUB_OUTPUT
            echo "has_changes=false" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          # Extraer valores con defaults
          nuevos=$(echo "$response" | jq -r '.reporte.documentos_nuevos // 0')
          actualizados=$(echo "$response" | jq -r '.reporte.documentos_actualizados // 0')
          
          # Validar que son números
          if ! [[ "$nuevos" =~ ^[0-9]+$ ]]; then nuevos=0; fi
          if ! [[ "$actualizados" =~ ^[0-9]+$ ]]; then actualizados=0; fi

          echo "nuevos=$nuevos" >> $GITHUB_OUTPUT
          echo "actualizados=$actualizados" >> $GITHUB_OUTPUT

          if [ "$nuevos" -gt 0 ] || [ "$actualizados" -gt 0 ]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi

      - name: Notificar cambios por Slack
        if: steps.check_nuevos.outputs.has_changes == 'true'
        uses: slackapi/slack-github-action@v1.25.0
        with:
          payload: |
            {
              "text": "🔔 Nuevos documentos MINEDUC detectados",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "📚 Actualización de Documentos MINEDUC"
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*Documentos nuevos:* ${{ steps.check_nuevos.outputs.nuevos }}\n*Documentos actualizados:* ${{ steps.check_nuevos.outputs.actualizados }}"
                  }
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": {
                        "type": "plain_text",
                        "text": "Ver en GitHub"
                      },
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK

  extract-rubricas:
    name: Extraer Rúbricas con IA
    runs-on: ubuntu-latest
    needs: monitor-documentos
    if: needs.monitor-documentos.outputs.has_changes == 'true' || github.event.inputs.force_rubric_extraction == 'true'
    timeout-minutes: 45
    outputs:
      extracted: ${{ steps.count_extracted.outputs.count }}

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias
        run: |
          pip install --upgrade pip
          pip install anthropic supabase python-dotenv pdfplumber tabula-py

      - name: Ejecutar extracción de rúbricas
        id: extract
        run: |
          python scripts/document-monitor/rubric-extractor.py --auto --verbose
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Contar rúbricas extraídas
        id: count_extracted
        run: |
          # Contar rúbricas desde logs o asumir valor basado en documentos nuevos
          extracted=${{ needs.monitor-documentos.outputs.nuevos || 0 }}
          if ! [[ "$extracted" =~ ^[0-9]+$ ]]; then extracted=0; fi
          echo "count=$extracted" >> $GITHUB_OUTPUT
          echo "Rúbricas extraídas: $extracted"

      - name: Guardar logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: extraction-logs
          path: logs/rubric-extraction-*.log
          retention-days: 30

  validate-data:
    name: Validar Calidad de Datos
    runs-on: ubuntu-latest
    needs: extract-rubricas
    if: always()
    timeout-minutes: 15
    outputs:
      critical_errors: ${{ steps.validate.outputs.critical_errors }}
      validated: ${{ steps.validate.outputs.validated }}

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.40.x

      - name: Ejecutar validación de chunks
        id: validate
        run: |
          deno run --allow-net --allow-env --allow-write scripts/validate-rag-data.ts
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Publicar resultados de validación
        run: |
          echo "### ✅ Validación de Datos RAG" >> $GITHUB_STEP_SUMMARY
          cat validation-results.md >> $GITHUB_STEP_SUMMARY

      - name: Fallar si hay errores críticos
        if: steps.validate.outputs.critical_errors > 0
        run: |
          echo "❌ Se encontraron ${{ steps.validate.outputs.critical_errors }} errores críticos"
          exit 1

  update-metrics:
    name: Actualizar Métricas RAG
    runs-on: ubuntu-latest
    needs: [monitor-documentos, extract-rubricas, validate-data]
    if: always()

    steps:
      - name: Registrar métricas en Supabase
        run: |
          curl -X POST \
            "${{ secrets.SUPABASE_URL }}/rest/v1/metricas_pipeline_rag" \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}" \
            -H "Content-Type: application/json" \
            -H "Prefer: return=representation" \
            -d '{
              "fecha": "'$(date -I)'",
              "documentos_monitoreados": ${{ needs.monitor-documentos.outputs.nuevos || 0 }},
              "documentos_procesados": ${{ needs.extract-rubricas.outputs.extracted || 0 }},
              "chunks_validados": ${{ needs.validate-data.outputs.validated || 0 }},
              "errores_criticos": ${{ needs.validate-data.outputs.critical_errors || 0 }},
              "workflow_run_id": "${{ github.run_id }}"
            }'

  notify-completion:
    name: Notificar Completado
    runs-on: ubuntu-latest
    needs: [monitor-documentos, extract-rubricas, validate-data, update-metrics]
    if: always()

    steps:
      - name: Determinar estado
        id: status
        run: |
          if [ "${{ needs.validate-data.result }}" == "failure" ]; then
            echo "status=❌ Falló" >> $GITHUB_OUTPUT
            echo "color=#dc2626" >> $GITHUB_OUTPUT
          elif [ "${{ needs.extract-rubricas.result }}" == "failure" ]; then
            echo "status=⚠️ Completado con errores" >> $GITHUB_OUTPUT
            echo "color=#f59e0b" >> $GITHUB_OUTPUT
          else
            echo "status=✅ Exitoso" >> $GITHUB_OUTPUT
            echo "color=#16a34a" >> $GITHUB_OUTPUT
          fi

      - name: Enviar notificación final
        if: ${{ github.event.inputs.slack_notification == 'true' }}
        uses: slackapi/slack-github-action@v1.25.0
        with:
          payload: |
            {
              "attachments": [
                {
                  "color": "${{ steps.status.outputs.color }}",
                  "blocks": [
                    {
                      "type": "header",
                      "text": {
                        "type": "plain_text",
                        "text": "${{ steps.status.outputs.status }} Sync Rúbricas MINEDUC"
                      }
                    },
                    {
                      "type": "section",
                      "fields": [
                        {
                          "type": "mrkdwn",
                          "text": "*Documentos Nuevos:*\n${{ needs.monitor-documentos.outputs.nuevos || 0 }}"
                        },
                        {
                          "type": "mrkdwn",
                          "text": "*Documentos Actualizados:*\n${{ needs.monitor-documentos.outputs.actualizados || 0 }}"
                        },
                        {
                          "type": "mrkdwn",
                          "text": "*Rúbricas Extraídas:*\n${{ needs.extract-rubricas.outputs.extracted || 0 }}"
                        },
                        {
                          "type": "mrkdwn",
                          "text": "*Errores Críticos:*\n${{ needs.validate-data.outputs.critical_errors || 0 }}"
                        }
                      ]
                    },
                    {
                      "type": "context",
                      "elements": [
                        {
                          "type": "mrkdwn",
                          "text": "Triggered by: ${{ github.event_name }} | Run: <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|#${{ github.run_number }}>"
                        }
                      ]
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
