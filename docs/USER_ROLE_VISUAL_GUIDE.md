# Guía Visual: Sistema de Gestión de Usuarios con Roles

## 🎯 Objetivo
Administrar usuarios desde el panel de administración `/admin/usuarios` con:
- Creación de usuarios con perfil automático
- Asignación de roles desde tabla `roles` de Supabase
- Vinculación automática con tabla `profiles`

## 🏗️ Arquitectura del Sistema

```
┌─────────────────────────────────────────────────────────────┐
│                    Panel Admin (/admin/usuarios)             │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  [📋 Lista de Usuarios]    [➕ Crear Usuario]               │
│                                                               │
│  Usuario 1  │ juan@example.cl  │ Admin     │ [Editar]       │
│  Usuario 2  │ maria@example.cl │ Usuario   │ [Editar]       │
│                                                               │
└─────────────────────────────────────────────────────────────┘
         │                              │
         │                              │
         ▼                              ▼
┌──────────────────────┐      ┌──────────────────────┐
│   GET /api/admin/    │      │   POST /api/admin/   │
│      usuarios        │      │      usuarios        │
└──────────────────────┘      └──────────────────────┘
         │                              │
         │                              │
         ▼                              ▼
┌─────────────────────────────────────────────────────────────┐
│                         Supabase                             │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │ auth.users   │────│  profiles    │───▶│    roles     │  │
│  │              │ 1:1│              │ N:1│              │  │
│  │ id           │    │ id           │    │ id           │  │
│  │ email        │    │ role_id ────────▶│ codigo       │  │
│  │ ...          │    │ role (legacy)│    │ nombre       │  │
│  └──────────────┘    │ nombre       │    │ permisos     │  │
│                      │ plan         │    │ activo       │  │
│                      │ creditos...  │    └──────────────┘  │
│                      └──────────────┘                       │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         Vista: profiles_with_roles                    │  │
│  │  (JOIN profiles + roles para queries optimizadas)     │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## 📋 Flujo de Creación de Usuario

```
┌──────────────────────────────────────────────────────────────────┐
│ 1. Admin hace clic en "Crear Usuario"                            │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│ 2. Modal CreateUserModal se abre con formulario:                 │
│    - Email (requerido)                                            │
│    - Contraseña (requerido)                                       │
│    - Nombre, Asignatura, Nivel (opcionales)                       │
│    - Plan (dropdown dinámico desde /api/admin/planes)            │
│    - Rol (dropdown dinámico desde /api/admin/roles)              │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│ 3. POST /api/admin/usuarios                                       │
│    {                                                              │
│      email, password, nombre, asignatura, nivel,                 │
│      plan: "free", roleId: "uuid-del-rol"                        │
│    }                                                              │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│ 4. API crea usuario en auth.users (Supabase Admin API)           │
│    - email_confirm: true (auto-confirmado)                       │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│ 5. Trigger handle_new_user() crea perfil base                    │
│    - profiles(id, email, role: 'user', role_id: default)         │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│ 6. API actualiza perfil con datos completos                      │
│    - nombre, asignatura, nivel                                    │
│    - plan (con créditos según plan)                              │
│    - role_id (UUID del rol seleccionado)                         │
│    - role (código del rol, sincronizado)                         │
└──────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌──────────────────────────────────────────────────────────────────┐
│ 7. Usuario creado exitosamente                                   │
│    ✅ Perfil completo                                            │
│    ✅ Rol asignado                                               │
│    ✅ Créditos configurados                                      │
└──────────────────────────────────────────────────────────────────┘
```

## 🎨 Componentes UI

### Menú Admin Agrupado
```
┌─────────────────────────┐
│ ProfeFlow               │
│ Panel Admin             │
├─────────────────────────┤
│ □ Dashboard             │
│                         │
│ GESTIÓN DE USUARIOS     │ ← Nuevo grupo
│ ├─ 👥 Usuarios          │
│ └─ 🛡️  Roles            │
│                         │
│ □ Planificaciones       │
│ □ Evaluaciones          │
│ □ Portafolios           │
│ □ Planes                │
│ ...                     │
└─────────────────────────┘
```

### Vista /admin/usuarios
```
┌───────────────────────────────────────────────────────────────┐
│ Gestión de Usuarios                     [➕ Crear Usuario]    │
├───────────────────────────────────────────────────────────────┤
│                                                                │
│ ┌─────────┐  ┌─────────┐  ┌─────────┐                        │
│ │ Total   │  │ Usuarios│  │ Admins  │                        │
│ │   150   │  │   145   │  │    5    │                        │
│ └─────────┘  └─────────┘  └─────────┘                        │
│                                                                │
│ [🔍 Buscar...] [Plan: Todos ▼] [Rol: Todos ▼]                │
│                                                                │
│ ┌────────────────────────────────────────────────────────────┐ │
│ │ Usuario    Email        Plan  Rol          Acciones        │ │
│ ├────────────────────────────────────────────────────────────┤ │
│ │ Juan P.    juan@...     Pro   Admin 🛡️    [Editar][Créd] │ │
│ │ María G.   maria@...    Free  Usuario     [Editar][Créd]  │ │
│ └────────────────────────────────────────────────────────────┘ │
│                                                                │
└───────────────────────────────────────────────────────────────┘
```

### Modal Crear Usuario
```
┌─────────────────────────────────────────────────────────────┐
│ Crear Nuevo Usuario                                    [✕]  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Email *                    │  Contraseña *                 │
│  ┌─────────────────────────┐│  ┌─────────────────────────┐ │
│  │ usuario@ejemplo.cl      ││  │ ••••••••                 │ │
│  └─────────────────────────┘│  └─────────────────────────┘ │
│                                                              │
│  Nombre Completo                                            │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ Nombre del usuario                                     │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                              │
│  Plan *                     │  Rol *                        │
│  ┌─────────────────────────┐│  ┌─────────────────────────┐ │
│  │ Free - $0          [▼] ││  │ Usuario            [▼]  │ │
│  └─────────────────────────┘│  └─────────────────────────┘ │
│                                                              │
│  Asignatura                 │  Nivel                        │
│  ┌─────────────────────────┐│  ┌─────────────────────────┐ │
│  │ Matemáticas             ││  │ 8° Básico               │ │
│  └─────────────────────────┘│  └─────────────────────────┘ │
│                                                              │
│  ℹ️  Se creará automáticamente un perfil asociado al        │
│     usuario con el rol y plan seleccionados. Los créditos   │
│     se asignarán según el plan elegido.                     │
│                                                              │
│                           [Cancelar]  [Crear Usuario]       │
└─────────────────────────────────────────────────────────────┘
```

## 🔐 Seguridad y Permisos

### Row Level Security (RLS)
```sql
-- Tabla profiles
✅ Usuarios solo ven su propio perfil
✅ Admin usa bypass con service role key

-- Tabla roles
✅ Usuarios autenticados ven roles activos
✅ Solo admin puede crear/modificar roles
```

### API Authorization
```typescript
// Todas las rutas admin verifican:
1. Usuario autenticado (auth.getUser())
2. Perfil existe
3. profile.role === 'admin'

// Operaciones privilegiadas usan:
createAdminClient() // Bypass RLS con service role
```

## 📊 Base de Datos

### Tabla: roles
```sql
id          uuid PRIMARY KEY
nombre      text UNIQUE
codigo      text UNIQUE
descripcion text
permisos    jsonb
activo      boolean
created_at  timestamp
updated_at  timestamp
```

### Tabla: profiles (actualizada)
```sql
id                            uuid PRIMARY KEY
role_id                       uuid → roles(id)  ← NUEVO
role                          text              ← LEGACY
email, nombre, asignatura...
creditos_planificaciones...
```

### Vista: profiles_with_roles
```sql
SELECT 
  p.*,
  r.codigo as role_codigo,
  r.nombre as role_nombre,
  r.permisos as role_permisos
FROM profiles p
LEFT JOIN roles r ON p.role_id = r.id
```

## 🚀 Ventajas del Nuevo Sistema

### Antes
```
profiles.role = 'admin'  // Texto hardcodeado
                         // Difícil de extender
                         // Sin permisos granulares
```

### Ahora
```
profiles.role_id → roles.id
                 ↓
      roles.nombre: "Administrador"
      roles.codigo: "admin"
      roles.permisos: ["usuarios.editar", ...]
      
      ✅ Roles dinámicos desde BD
      ✅ Permisos configurables
      ✅ Fácil agregar nuevos roles
      ✅ Sistema escalable
```

## 📝 Ejemplo de Uso

### Crear un nuevo rol personalizado
```sql
INSERT INTO roles (nombre, codigo, descripcion, permisos, activo)
VALUES (
  'Coordinador',
  'coordinator',
  'Coordinador pedagógico con permisos especiales',
  '["planificaciones.ver_todas", "evaluaciones.ver_todas"]'::jsonb,
  true
);
```

### Asignar rol al crear usuario
```javascript
// El nuevo rol aparece automáticamente en el selector
// Sin necesidad de cambiar código frontend
fetch('/api/admin/usuarios', {
  method: 'POST',
  body: JSON.stringify({
    email: 'coordinador@colegio.cl',
    password: '...',
    roleId: 'uuid-del-rol-coordinator'
  })
})
```

## ✅ Checklist de Implementación

- [x] Migración de base de datos creada
- [x] Vista `profiles_with_roles` implementada
- [x] API POST para crear usuarios
- [x] API GET actualizada para incluir roles
- [x] API PUT actualizada para asignar roles
- [x] Componente `CreateUserModal` creado
- [x] Componente `EditUserModal` actualizado
- [x] Tabla de usuarios muestra nombre de rol
- [x] Sidebar agrupa Usuarios y Roles
- [x] Tipos TypeScript actualizados
- [x] Documentación completa
- [x] Script de verificación

## 🎓 Resumen

Este sistema permite gestionar usuarios de forma completa y profesional:

1. **Creación**: Usuarios se crean con un clic, con perfil y rol automáticos
2. **Flexibilidad**: Roles configurables desde la base de datos
3. **Seguridad**: RLS y autorización por rol
4. **Escalabilidad**: Fácil agregar nuevos roles sin tocar código
5. **UX**: Interfaz intuitiva para administradores

El sistema está listo para producción siguiendo las mejores prácticas de aplicaciones SaaS modernas.
